name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel superseded runs on the same ref (e.g. rapid pushes to a PR).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# Stages: lint → test → coverage → build. A later stage only starts once its
# dependency is green.
#
# The end-to-end suite (e2e/) deliberately does NOT run here — it used to, but
# spinning up a real Electron app on every push/PR was slow and not something
# we want blocking routine iteration. It now runs exactly once per release,
# as the test-gate job in main.yml (the tag-triggered build-and-publish
# workflow) — a red e2e run there blocks the build/publish steps the same way
# it blocked `build` here before. Run it locally any time with `npm run test:e2e`.
jobs:
  # ── Stage 1: lint (syntax) + dependency audit ───────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Syntax / lint check
        run: npm run lint
      # Reports known vulnerabilities. Non-blocking: a new upstream advisory
      # shouldn't turn every PR red. Drop continue-on-error to make it a gate.
      - name: Security audit
        run: npm audit --audit-level=high
        continue-on-error: true

  # ── Stage 2: unit tests (only if lint passed) ───────────────────────────────
  test:
    name: Unit tests
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Run tests
        run: npm test

  # ── Stage 3: coverage tracking (only if tests passed) ───────────────────────
  coverage:
    name: Coverage
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Coverage report
        run: npm run coverage

  # ── Stage 4: build matrix (only if coverage passed) ──────────────────────────
  build:
    name: Build (${{ matrix.os }})
    needs: coverage
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: --win --x64
            artifact: dist/*.exe
          - os: macos-latest
            target: --mac
            artifact: dist/*.dmg
          - os: ubuntu-latest
            target: --linux
            artifact: dist/*.AppImage
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      # Full install: postinstall rebuilds the native sqlite3 module for Electron.
      - name: Install dependencies
        run: npm ci
      # The build bundles .env and data/ via extraResources (package.json "build").
      # Both are gitignored, so CI provides stubs — we only verify packaging works,
      # not that the real SDE is present (releases run `npm run build-*` for that).
      - name: Stub build inputs
        shell: bash
        run: |
          cp .env.example .env
          mkdir -p data
          : > data/sde.sql
      - name: Build
        shell: bash
        run: npx --no-install electron-builder ${{ matrix.target }} --publish never
      - name: Verify artifact exists
        shell: bash
        run: |
          shopt -s nullglob
          files=(${{ matrix.artifact }})
          if [ ${#files[@]} -eq 0 ]; then
            echo "::error::Build produced no artifact matching '${{ matrix.artifact }}'"
            ls -la dist || true
            exit 1
          fi
          echo "Built artifact(s): ${files[*]}"
